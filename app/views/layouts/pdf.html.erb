<!DOCTYPE html>
<html lang="<%= current_language %>">
<head>
  <meta charset="utf-8" />
  <title><%=h html_title %></title>
  <meta name="description" content="<%= Redmine::Info.app_name %>" />
  <meta name="keywords" content="issue,bug,tracker" />
  <%= csrf_meta_tag %>

  <meta http-equiv="content-type" content="text/html; charset=utf-8" />

  <%= favicon %>
  <%= javascript_heads %>
  <%= heads_for_theme %>
  <%= yield :header_tags -%>
</head>
<body class="<%=h body_css_classes %>">
      <!-- style="background: url('file://< "#{WickedPdfHelper.root_path.join('public', 'plugin_assets', 'redmine_events', 'images')}/cmvoa_footer.jpg" %>') bottom left no-repeat fixed;" -->
  <div>
    <%= yield %>
  </div>
</body>
</html>
